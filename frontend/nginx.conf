server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    location /api/ {
        proxy_pass http://infotek-personkort-backend;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

